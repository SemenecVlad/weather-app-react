React application that gives you the ability to see your local weather with 5day/3hour forecast. Also you can find a weather in any city you want.

Steps for runing project:

1) npm install or yarn install

2) npm start or yarn start
for running an app in Dev mode
or npm run build or yarn build for Production build
for serving project in Production mode you need to install 'serve' - npm i -g serve
Then 'serve -s build'
